import { MyFormatDataPipe } from './my-format-data.pipe';

describe('MyFormatDataPipe', () => {
  it('create an instance', () => {
    const pipe = new MyFormatDataPipe();
    expect(pipe).toBeTruthy();
  });
});
